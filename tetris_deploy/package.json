{
  "name": "tetristributed",
  "version": "1.0.0",
  "description": "Distributed multiplayer Tetris game",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && npm start"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
